apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "yolvo.fullname" . }}-test-connection"
  labels:
    {{- include "yolvo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "yolvo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
